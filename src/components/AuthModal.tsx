"use client";
import { useState } from "react";
import { motion } from "framer-motion";
import { X, LockKeyhole } from "lucide-react";
import { authenticate, setSession } from "@/src/lib/auth";
import { CharacterStage } from "@/src/components/CharacterStage";
export function AuthModal({onClose,onSuccess}:{onClose:()=>void;onSuccess:()=>void}){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [loading,setLoading]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setError("");setLoading(true);try{const s=await authenticate(email,password);setSession(s);onSuccess()}catch(err){setError(err instanceof Error?err.message:"Unable to authenticate.")}finally{setLoading(false)}}return <motion.div className="modal-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><motion.div className="auth-modal" initial={{opacity:0,y:24,scale:.98}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:20}}><button className="close-button" onClick={onClose}><X size={18}/></button><div className="auth-art"><CharacterStage character="guide" bubble="Your build starts here."/></div><div className="auth-form"><div className="eyebrow"><LockKeyhole size={14}/> Secure sign in</div><h2>Welcome back.</h2><p>Authenticate to open your Forge-IT workspace.</p><form onSubmit={submit}><label>Email address<input value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email"/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••" type="password" autoComplete="current-password"/></label>{error&&<div className="error-text">{error}</div>}<button className="primary-button wide" disabled={loading}>{loading?"Opening workspace…":"Continue to dashboard"}</button></form><div className="auth-note">Demo mode is enabled by default. Supabase-ready auth can replace it via env configuration.</div></div></motion.div></motion.div>}
