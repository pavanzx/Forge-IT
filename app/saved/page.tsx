"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { AppShell } from "@/src/components/AppShell"; import { CharacterStage } from "@/src/components/CharacterStage"; import { tools } from "@/src/data/tools";
export default function Saved(){const router=useRouter();const [items,setItems]=useState(tools.slice(0,5));return <AppShell active="saved" character="scout"><section className="module-page"><div className="page-heading"><div><span className="section-kicker">YOUR SHORTLIST</span><h1>Saved Tools</h1><p>Keep the technologies you’re considering close to your project.</p></div><CharacterStage character="scout" bubble="These are your saved options. I can compare them anytime." /></div><div className="saved-list">{items.map(t=><div className="saved-row" key={t.name}><div className="tool-logo">{t.name[0]}</div><div><b>{t.name}</b><p>{t.category} · {t.match}% match</p></div><div className="hero-actions"><button className="secondary-button" onClick={()=>router.push('/tools')}>Open</button><button className="ghost-button" onClick={()=>setItems(v=>v.filter(x=>x.name!==t.name))}>Remove</button></div></div>)}</div></section></AppShell>}
