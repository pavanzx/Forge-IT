"use client";
import { useParams,useState } from "next/navigation"; import { AppShell } from "@/src/components/AppShell"; import { CharacterStage } from "@/src/components/CharacterStage";
export default function Page(){const {taskId}=useParams<{taskId:string}>();const [done,setDone]=useState(false);return <AppShell active="tasks" character="alert"><section className="module-page"><div className="page-heading"><div><span className="section-kicker">TASK {taskId}</span><h1>Implement Authentication</h1><p>Set up the authentication layer, protect routes, then verify session persistence.</p></div><CharacterStage character="alert" bubble={done?'Great — this task is complete.':'Check the dependency order before you mark this complete.'}/></div><div className="task-board"><article className={`task ${done?'complete':''}`}><button className="check" onClick={()=>setDone(v=>!v)}>{done?'✓':''}</button><div><b>{done?'Completed':'In progress'}</b><p>{done?'Saved in this demo session.':'Complete the steps, then verify the app still redirects correctly.'}</p></div><button className="primary-button" onClick={()=>setDone(true)}>Mark complete</button></article></div></section></AppShell>}
