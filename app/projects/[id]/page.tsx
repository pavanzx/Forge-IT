"use client";
import { useParams,useRouter } from "next/navigation"; import { AppShell } from "@/src/components/AppShell"; import { CharacterStage } from "@/src/components/CharacterStage"; import { useState } from "react";
export default function Page(){const {id}=useParams<{id:string}>();const router=useRouter();const [tab,setTab]=useState('overview');return <AppShell active="projects" character="analyst"><section className="module-page"><div className="page-heading"><div><span className="section-kicker">PROJECT {id}</span><h1>AI Study Assistant</h1><p>A build-ready project workspace connecting requirements, recommendations, roadmap, tasks, timeline, and analytics.</p></div><CharacterStage character="analyst" bubble="I’ll keep the project context consistent while you work." /></div><div className="toolbar">{['overview','requirements','recommendations','roadmap','timeline','analytics'].map(x=><button key={x} className={tab===x?'primary-button':'secondary-button'} onClick={()=>setTab(x)}>{x}</button>)}</div><div className="panel"><h2>{tab[0].toUpperCase()+tab.slice(1)}</h2><p className="muted">This project view stays connected to the same Forge-IT workflow.</p><div className="hero-actions"><button className="primary-button" onClick={()=>router.push(`/projects/${id}/tasks/next`)}>Open current task</button><button className="secondary-button" onClick={()=>router.push('/assistant')}>Ask Nexa</button></div></div></section></AppShell>}
