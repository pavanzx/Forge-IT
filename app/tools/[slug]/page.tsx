"use client";
import { useParams,useRouter } from "next/navigation"; import { AppShell } from "@/src/components/AppShell"; import { CharacterStage } from "@/src/components/CharacterStage"; import { tools } from "@/src/data/tools";
export default function Page(){const p=useParams<{slug:string}>();const router=useRouter();const t=tools.find(x=>x.name.toLowerCase().replace(/\s+/g,'-')===p.slug)||tools[0];return <AppShell active="tools" character="scout"><section className="module-page"><div className="page-heading"><div><span className="section-kicker">TOOL DETAIL</span><h1>{t.name}</h1><p>{t.description}</p></div><CharacterStage character="scout" bubble="Want an alternative? I can compare this with another tool." /></div><div className="metrics"><div className="metric-card"><b>{t.match}%</b><span>Project fit</span></div><div className="metric-card"><b>{t.category}</b><span>Category</span></div><div className="metric-card"><b>Ready</b><span>Demo state</span></div><button className="metric-card" onClick={()=>router.push('/saved')}><b>Save</b><span>Add to shortlist</span></button></div></section></AppShell>}
