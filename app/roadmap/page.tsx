"use client";
import { useState } from "react"; import { AppShell } from "@/src/components/AppShell"; import { CharacterStage } from "@/src/components/CharacterStage";
export default function Roadmap(){const [done,setDone]=useState(2);const phases=["Planning","Setup","Design","Development","Testing","Deployment","Launch"];return <AppShell active="roadmap" character="alert"><section className="module-page"><div className="page-heading"><div><span className="section-kicker">PLAN WITH CONFIDENCE</span><h1>Project Roadmap</h1><p>Phase sequencing, dependencies, and a visible next milestone.</p></div><CharacterStage character="alert" bubble="I’ll warn you before a dependency becomes a blocker." /></div><div className="timeline">{phases.map((p,i)=>{const pct=i<done?100:i===done?68:0;return <button type="button" className="timeline-row" key={p} onClick={()=>setDone(Math.max(done,i))}><span className={i<done?"phase-number done":"phase-number"}>{i+1}</span><div><b>{p}</b><p>{i<done?"Completed":i===done?"In Progress":"Not Started"}</p></div><div className="bar"><span style={{width:`${pct}%`}}/></div><em>{pct}%</em></button>})}</div></section></AppShell>}
