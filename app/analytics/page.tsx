"use client";
import { useState } from "react"; import { AppShell } from "@/src/components/AppShell"; import { CharacterStage } from "@/src/components/CharacterStage";
export default function Analytics(){const [range,setRange]=useState('7d');const [report,setReport]=useState(false);return <AppShell active="analytics" character="analyst"><section className="module-page"><div className="page-heading"><div><span className="section-kicker">MEASURE WHAT MATTERS</span><h1>Analytics</h1><p>Project momentum, completion trends, and AI-assisted decisions.</p></div><CharacterStage character="analyst" bubble="Your completion trend is improving. Watch blockers next." /></div><div className="toolbar"><select value={range} onChange={e=>setRange(e.target.value)}><option value="7d">Last 7 days</option><option value="30d">Last 30 days</option><option value="90d">Last 90 days</option></select><button className="secondary-button" onClick={()=>setReport(v=>!v)}>{report?'Report ready ✓':'Generate report'}</button></div><div className="metrics">{[["12","Total Projects"],["68","Tasks Completed"],["72%","Completion Rate"],["48h 30m","Time Spent"]].map(([v,l])=><div className="metric-card" key={l}><b>{v}</b><span>{l}</span></div>)}</div><div className="chart-panel"><div className="bars">{[35,46,40,58,64,72,84].map((v,i)=><div key={i} className="bar-col"><span style={{height:`${v}%`}}/><small>W{i+1}</small></div>)}</div><div className="insight-box"><b>AI Insight</b><p>Projects move faster when the next task is explicit and dependencies are resolved before work starts.</p></div></div></section></AppShell>}
