"use client";
import { useState } from "react"; import { AppShell } from "@/src/components/AppShell"; import { CharacterStage } from "@/src/components/CharacterStage";
export default function Reports(){const [exported,setExported]=useState(false);return <AppShell active="reports" character="analyst"><section className="module-page"><div className="page-heading"><div><span className="section-kicker">PROJECT REPORTING</span><h1>Reports</h1><p>Summaries of progress, risks, decisions, and AI-assisted work.</p></div><CharacterStage character="analyst" bubble="I can turn your activity into a readable weekly report." /></div><div className="report-grid">{[["Projects","12"],["Completed Tasks","68"],["AI Interactions","156"],["Blocked Tasks","2"]].map(([k,v])=><div className="metric-card" key={k}><b>{v}</b><span>{k}</span></div>)}</div><div className="report-panel"><h2>Weekly summary</h2><p>Completion rose steadily across active projects. The next improvement opportunity is reducing blocked dependencies and completing the highest-value tasks first.</p><button className="primary-button" onClick={()=>setExported(true)}>{exported?'Report exported ✓':'Export Report'}</button></div></section></AppShell>}
