export function initAmbient(){const root=document.getElementById('ambient-layer');root.innerHTML='<div class="ambient-grid"></div><div class="ambient-vignette"></div>';for(let i=0;i<26;i++){const p=document.createElement('i');p.className='particle';p.style.left=Math.random()*100+'%';p.style.top=Math.random()*100+'%';p.style.animationDuration=(8+Math.random()*14)+'s';p.style.animationDelay=(-Math.random()*12)+'s';root.append(p)}}
