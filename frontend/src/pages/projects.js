import {store} from '../state/store.js';import {projectCard} from '../components/project-card.js';export function projects(){return `<div class="page-head"><div><div class="kicker">MY PROJECTS</div><h1 class="title">Your Forge Projects</h1><p class="subtitle">Every idea, roadmap and task in one workshop.</p></div><button class="btn primary" data-nav="idea">+ New Project</button></div><div class="page-body"><div class="grid grid-3">${store.state.projects.map(p=>`<div>${projectCard(p)}<button class="btn w-full mt-2" data-project="${p.id}">Open Project →</button></div>`).join('')}</div></div>`}
