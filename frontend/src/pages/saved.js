import {store} from '../state/store.js';export function saved(){return `<div class="page-head"><div><div class="kicker">SAVED TOOLS</div><h1 class="title">Your Toolbelt</h1><p class="subtitle">Tools Scout has helped you keep close.</p></div></div><div class="page-body"><div class="grid grid-2">${store.state.savedTools.map(t=>`<div class="card"><div class="row-main"><div class="icon-tile">${t[0]}</div><div><b>${t}</b><div class="small muted">Saved for ${store.project.name}</div></div></div></div>`).join('')||'<div class="empty">No saved tools yet.</div>'}</div></div>`}
