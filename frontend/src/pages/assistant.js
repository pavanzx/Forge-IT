import {store} from '../state/store.js';import {message} from '../components/chat-message.js';import {bus} from '../events/bus.js';export function assistant(){const chat=store.state.chat.length?store.state.chat:[{role:'assistant',text:'I can help you choose the best next step for your project.'}];return `<div class="page-head"><div><div class="kicker">10 AI ASSISTANT</div><h1 class="title">Your AI Co-Pilot</h1><p class="subtitle">Context-aware guidance from idea to launch.</p></div><button class="btn" data-character="bot">Invoke Bot</button></div><div class="page-body grid grid-2"><div class="card"><div class="chat">${chat.map(x=>message(x.role,x.text)).join('')}</div><div class="chat-compose"><input id="chat-input" class="input" placeholder="Ask me anything..."><button class="btn primary" id="send-chat">→</button></div></div><div class="card dark"><img src="assets/bot.png" style="height:190px;margin:10px auto" alt="Bot"><div class="eyebrow" style="color:#a8afaa;text-align:center">BOT STATUS</div><h3 style="color:#fff;text-align:center">Online · Project Context Loaded</h3><p style="color:#b9bfbc;text-align:center;font-size:10px">${store.project.name} · ${store.project.progress}% complete</p></div></div>`}
