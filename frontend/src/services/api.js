import {CONFIG} from '../config.js';export class API{constructor(base=CONFIG.API_BASE){this.base=base}async request(path,options={}){const res=await fetch(this.base+path,{headers:{'Content-Type':'application/json',...(options.headers||{})},...options});if(!res.ok)throw new Error(`API ${res.status}`);const text=await res.text();return text?JSON.parse(text):null}health(){return this.request('/health')}projects(){return this.request('/projects')}createProject(data){return this.request('/projects/quick-create',{method:'POST',body:JSON.stringify(data)})}tasks(id){return this.request(`/tasks/project/${id}`)}requirements(id){return this.request(`/requirements/project/${id}`)}}export const api=new API();
