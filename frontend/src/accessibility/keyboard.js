import {router} from '../core/router.js';import {characterDirector} from '../characters/director.js';export function initKeyboard(){window.addEventListener('keydown',e=>{if(e.key==='/'&&!['INPUT','TEXTAREA'].includes(document.activeElement.tagName)){e.preventDefault();document.getElementById('global-search')?.focus()}if(e.key.toLowerCase()==='g'&&e.shiftKey){characterDirector.invoke('guide',{message:'Guide is ready. Choose a destination.'})}})}
