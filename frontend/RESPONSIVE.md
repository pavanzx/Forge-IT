# Responsive Behavior

Desktop uses a persistent sidebar and multi-column cards. Tablet collapses navigation. Mobile uses a bottom navigation bar and single-column content. Touch targets remain large enough for normal interaction.
